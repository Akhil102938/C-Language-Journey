#include<stdio.h>
#include<string.h>
int main()
{
    char a[30];
    char b[30];
    char c[30]="akhil@102938";
    printf("Enter Email ID:- ");
    scanf("%s",&a);
    printf("Enter password:- ");
    scanf("%s",&b);
    strcmp(b,c);
    if(strcmp(b,c)==0)
    {
        printf("Access Granted");
    }
    else
    {
        printf("Access Denied");
    }
    return 0;
}
