#include<stdio.h>

int main()
{
    char a[30];
    int i = 0;

    printf("Enter your Name: ");
    scanf("%s", a);

    while(a[i] != '\0')
    {
        i++;
    }

    i = i - 1;

    printf("Reverse String: ");

    for(i=i; i>=0; i--)
    {
        printf("%c", a[i]);
    }

    return 0;
}
