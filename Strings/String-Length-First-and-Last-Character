#include<stdio.h>
#include<string.h>

int main()
{
    char a[20] = "akhil";
    int len;

    printf("%s\n", a);

    len = strlen(a);

    printf("string length:- %d\n", len);
    printf("first letter:- %c\n", a[0]);
    printf("last letter:- %c\n", a[len-1]);

    return 0;
}
